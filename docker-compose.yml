services:
  directus:
    image: docker.io/directus/directus:11@sha256:ae6ab737fd04077d295bbefa545cc4aefccc206e3d0120c83812f9b482a8c9a5
    ports:
      - 8055:8055
    volumes:
      - database:/directus/database
      - uploads:/directus/uploads
      - .:/directus/extensions/directus-extension-blurhasher
      - .:/directus/node_modules/directus-extension-blurhasher
    environment:
      KEY: "replace-with-random-value"
      SECRET: "replace-with-random-value"
      ADMIN_EMAIL: "admin@example.com"
      ADMIN_PASSWORD: "admin"
      DB_CLIENT: "sqlite3"
      DB_FILENAME: "/directus/database/data.db"
      ASSETS_TRANSFORM_IMAGE_MAX_DIMENSION: 10000

volumes:
  database:
  uploads:
